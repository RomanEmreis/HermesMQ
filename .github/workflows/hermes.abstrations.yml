name: Hermes.Abstrations (.NET Core)
env:
  proj_folder: src/Hermes.Abstractions

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: Setup .NET Core
      uses: actions/setup-dotnet@v1
      with:
        dotnet-version: 3.1.101
    - name: Install dependencies
      run: dotnet restore {{proj_folder}}
    - name: Build
      run: dotnet build {{proj_folder}} --configuration Release --no-restore
